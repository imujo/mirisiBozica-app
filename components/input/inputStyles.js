import { StyleSheet } from "react-native";

const inputStyles = StyleSheet.create({
  title: {
    fontSize: 14,
    fontWeight: "500",
  },
  inputValue: {
    color: "gray",
    fontWeight: "400",
    fontSize: 12,
  },
});

export default inputStyles;
